const Password = () => {
  return (
    <>
      <label htmlFor="password-field">Password</label>
      <input
        id="password-field"
        name="password"
        type="password"
        className="password"
      />
    </>
  );
};

export default Password;
